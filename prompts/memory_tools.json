{
  "recall_memories": {
    "description": "Search and recall relevant memories from long-term memory based on semantic similarity. This tool implements RAG-like functionality to retrieve past experiences and knowledge.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The query to search for relevant memories. Can be a question, topic, or description of what you're looking for."
        },
        "top_k": {
          "type": "integer",
          "description": "Number of most relevant memories to retrieve. Defaults to 5.",
          "default": 5
        }
      },
      "required": ["query"]
    }
  },
  "recall_memories_by_time": {
    "description": "Search and recall memories from a specific time period. Useful for finding what happened 'yesterday', 'last week', or on a specific date.",
    "parameters": {
      "type": "object",
      "properties": {
        "time_query": {
          "type": "string",
          "description": "Time-based query such as 'yesterday', 'last week', 'this month', or specific dates like '2024-01-01'"
        },
        "top_k": {
          "type": "integer",
          "description": "Number of memories to retrieve from that time period. Defaults to 5.",
          "default": 5
        }
      },
      "required": ["time_query"]
    }
  },
  "get_memory_summary": {
    "description": "Get a summary of the long-term memory system status, including total number of memories, storage information, and system health.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "query_skill": {
    "description": "Search and retrieve relevant skills from the skill library based on semantic similarity. Returns the top 3 most relevant skills with their complete content including skill_id, title, usage conditions, quality index, and detailed content. Use this tool to find historical experiences and skills that might help complete the current task more efficiently.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The query to search for relevant skills. Can be a task description, problem statement, or description of what you're looking for."
        }
      },
      "required": ["query"]
    }
  },
  "rate_skill": {
    "description": "Rate the quality of a skill after using it. Updates the skill's quality index using weighted average. Use this tool after completing a task to provide feedback on how useful the skill was.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The ID of the skill to rate (obtained from query_skill results)"
        },
        "rating": {
          "type": "number",
          "description": "Rating value between 0.0 and 1.0, where 1.0 means the skill was very useful and 0.0 means it was not useful"
        }
      },
      "required": ["skill_id", "rating"]
    }
  },
  "edit_skill": {
    "description": "Edit a skill file. Parameters are identical to edit_file tool. Use this to update skill content, usage conditions, or other information.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The ID of the skill to edit"
        },
        "edit_mode": {
          "type": "string",
          "description": "Edit mode: 'full_replace' to replace entire content, 'append' to add to end, 'lines_replace' to replace specific lines",
          "enum": ["full_replace", "append", "lines_replace"]
        },
        "code_edit": {
          "type": "string",
          "description": "The content to edit or add"
        },
        "old_code": {
          "type": "string",
          "description": "For lines_replace mode, the old code to replace"
        }
      },
      "required": ["skill_id", "edit_mode", "code_edit"]
    }
  },
  "delete_skill": {
    "description": "Delete a skill file by moving it to the legacy directory. The skill will no longer be available for query but is preserved for reference.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The ID of the skill to delete"
        }
      },
      "required": ["skill_id"]
    }
  },
  "copy_skill_files": {
    "description": "Copy files to a skill's code backup directory. Use this to backup successful code files and documents related to a skill. The model should analyze manager.out logs to determine which files are valid outputs that should be backed up.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill_id": {
          "type": "string",
          "description": "The ID of the skill to associate files with"
        },
        "file_paths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of file paths (relative to workspace) to copy to the skill's backup directory. Should include code files (.py, .js, .ts, .java, etc.) and document files (.md, .txt, etc.), but exclude configuration files (.json, .yaml, etc.) and image files."
        }
      },
      "required": ["skill_id", "file_paths"]
    }
  }
} 