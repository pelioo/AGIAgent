---
name: fullstack-development
description: 指导AI进行完整的全栈开发，从项目规划、技术选型、架构设计到前后端实现、测试部署的完整流程。涵盖Web应用、移动端、小程序、API服务、数据库设计、DevOps等全栈技术栈，支持快速交付高质量、可维护、可扩展的应用系统。
---

# 全栈开发技能

## 任务目标

- **本Skill用于**：指导AI完成完整的全栈开发任务
- **能力包含**：项目规划、技术选型、架构设计、前后端实现、测试部署、运维监控
- **触发条件**：当需要开发完整的应用程序时，或需要进行前后端集成开发时

## 核心概念：全栈开发的三层架构

### 应用架构分层

全栈开发遵循经典的三层架构模式：

```
┌─────────────────────────────────────────────────────────────┐
│                        展示层 (Presentation)                  │
│   Web / 移动端 / 小程序 / 桌面应用 / API文档                    │
├─────────────────────────────────────────────────────────────┤
│                        业务逻辑层 (Business Logic)              │
│   API服务 / 业务规则 / 工作流 / 数据处理 / 认证授权               │
├─────────────────────────────────────────────────────────────┤
│                        数据访问层 (Data Access)                │
│   数据库 / 缓存 / 文件存储 / 搜索引擎 / 第三方服务                 │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈全景图

```
前端技术栈：
├── Web应用：Vue/React/Angular + TypeScript
├── 移动端：React Native / Flutter / Swift / Kotlin
├── 小程序：微信小程序 / 支付宝小程序 / 抖音小程序
├── 桌面端：Electron / Tauri / NW.js
└── 静态站点：Next.js / Nuxt.js / Astro

后端技术栈：
├── Web框架：Express / Fastify / NestJS / Django / FastAPI / Spring Boot
├── 微服务：gRPC / GraphQL / REST
├── 认证：JWT / OAuth2 / SSO
└── 实时通信：WebSocket / Socket.io / SSE

数据层：
├── 关系型：PostgreSQL / MySQL / MariaDB
├── NoSQL：MongoDB / Redis / Cassandra
├── 向量数据库：Pinecone / Milvus / Qdrant
├── 搜索引擎：Elasticsearch / Meilisearch
└── 缓存：Redis / Memcached

DevOps：
├── 容器化：Docker / Podman
├── 编排：Kubernetes / Docker Compose
├── CI/CD：GitHub Actions / GitLab CI / Jenkins
├── 监控：Prometheus / Grafana / ELK
└── 云服务：AWS / GCP / Azure / 阿里云 / 腾讯云
```

## 全栈开发流程

### 标准开发流程

```
┌──────────────┐
│  1. 需求分析  │ ← 理解用户需求、明确功能范围
└──────┬───────┘
       ↓
┌──────────────┐
│  2. 技术选型  │ ← 选择合适的技术栈和工具
└──────┬───────┘
       ↓
┌──────────────┐
│  3. 架构设计  │ ← 设计系统架构和模块划分
└──────┬───────┘
       ↓
┌──────────────┐
│  4. 数据库设计 │ ← 设计数据模型和表结构
└──────┬───────┘
       ↓
┌──────────────┐
│  5. API设计   │ ← 定义接口规范和协议
└──────┬───────┘
       ↓
┌──────────────┐
│  6. 前端开发  │ ← 实现用户界面和交互
└──────┬───────┘
       ↓
┌──────────────┐
│  7. 后端开发  │ ← 实现业务逻辑和数据处理
└──────┬───────┘
       ↓
┌──────────────┐
│  8. 集成测试  │ ← 前后端联调和系统测试
└──────┬───────┘
       ↓
┌──────────────┐
│  9. 部署上线  │ ← 部署到生产环境
└──────┬───────┘
       ↓
┌──────────────┐
│  10. 运维监控 │ ← 监控和持续优化
└──────────────┘
```

## 阶段详解

### 阶段1：需求分析

#### 收集需求信息

**功能需求**：
- 核心功能列表（必须实现的功能）
- 辅助功能列表（可选功能）
- 用户角色和权限划分
- 数据处理流程

**非功能需求**：
- 性能指标（响应时间、并发量）
- 可用性要求（99.9% uptime）
- 安全要求（数据加密、访问控制）
- 兼容性要求（浏览器、设备）

**约束条件**：
- 时间限制
- 预算限制
- 技术限制
- 法规限制

#### 输出物：需求规格说明书

```markdown
# 项目名称
## 功能需求
1. 用户管理
   - 注册/登录
   - 权限管理
2. 核心功能
   - XX功能
   - YY功能
## 非功能需求
- 性能：响应时间 < 200ms
- 安全：数据加密存储
- 兼容性：支持Chrome/Firefox/Safari
```

### 阶段2：技术选型

#### 前端技术选型

| 场景 | 推荐技术栈 | 适用情况 |
|------|-----------|---------|
| 快速原型 | HTML + TailwindCSS + Vanilla JS | 简单页面，无需复杂交互 |
| 企业后台 | React + Ant Design / Vue + Element Plus | 中后台系统，注重表单和表格 |
| 高性能Web | React + Next.js / Vue + Nuxt.js | SEO敏感，需要SSR |
| 移动端优先 | React Native / Flutter | 需要原生体验 |
| 小程序 | Taro / uni-app | 多平台小程序 |
| 实时应用 | React + Socket.io | 聊天、协作、游戏 |

#### 后端技术选型

| 场景 | 推荐技术栈 | 适用情况 |
|------|-----------|---------|
| 轻量API | Node.js + Express/Fastify | 轻量级服务，JSON处理 |
| 企业应用 | Java + Spring Boot | 企业级，稳定可靠 |
| 数据密集 | Python + FastAPI/Django | 数据处理，ML集成 |
| 实时处理 | Node.js + Socket.io / Go | 高并发，WebSocket |
| 微服务 | Go + gRPC / Java + Spring Cloud | 大型分布式系统 |
| Serverless | Node.js/Python + 云函数 | 事件驱动，按需扩展 |

#### 数据库选型

| 数据类型 | 推荐数据库 | 理由 |
|---------|-----------|------|
| 关系数据 | PostgreSQL | 功能强大，支持JSON |
| 高性能KV | Redis | 内存数据库，极速响应 |
| 文档存储 | MongoDB | 灵活Schema，适合敏捷开发 |
| 搜索引擎 | Elasticsearch/Meilisearch | 全文搜索，日志分析 |
| 向量数据 | Pinecone/Milvus | AI向量检索 |
| 时序数据 | InfluxDB/TimescaleDB | 监控、IoT数据 |

### 阶段3：架构设计

#### 系统架构设计原则

**高内聚低耦合**：
- 模块职责单一
- 模块间依赖最小化
- 通过接口通信

**可扩展性**：
- 水平扩展：增加服务器实例
- 垂直扩展：提升单机性能
- 功能扩展：插件化设计

**可维护性**：
- 代码规范统一
- 文档完整
- 自动化测试覆盖

**安全性**：
- 纵深防御
- 最小权限
- 数据加密

#### 架构图模板

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端层                                 │
│    ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐          │
│    │  Web    │  │  移动端  │  │  小程序  │  │  API    │          │
│    └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘          │
└─────────┼────────────┼────────────┼────────────┼────────────────┘
          │            │            │            │
          └────────────┴────────────┼────────────┘
                                    │ HTTPS
┌────────────────────────────────────┼────────────────────────────────┐
│                            负载均衡层                              │
│                         ┌──────────┴──────────┐                   │
│                         │     Nginx / 云负载均衡  │                   │
│                         └──────────┬──────────┘                   │
└────────────────────────────────────┼────────────────────────────────┘
                                    │
┌────────────────────────────────────┼────────────────────────────────┐
│                            应用服务层                              │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│   │  Web服务1 │  │  Web服务2 │  │  API服务  │  │  定时任务 │          │
│   └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────────┘          │
│        │             │             │                              │
│        └─────────────┴──────┬──────┘                              │
└─────────────────────────────┼──────────────────────────────────────┘
                              │
┌─────────────────────────────┼──────────────────────────────────────┐
│                            缓存层                                   │
│                    ┌────────┴────────┐                            │
│                    │  Redis集群       │                            │
│                    │  Session/缓存   │                            │
│                    └────────┬────────┘                            │
└─────────────────────────────┼──────────────────────────────────────┘
                              │
┌─────────────────────────────┼──────────────────────────────────────┐
│                            数据层                                   │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│   │ PostgreSQL│  │ MongoDB  │  │ Elasticsearch│  │ 对象存储  │         │
│   │  主数据库  │  │ 文档存储  │  │  搜索引擎  │  │  文件存储  │         │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────────────────────┘
```

### 阶段4：数据库设计

#### 设计规范

**命名规范**：
- 表名：复数形式，`users`, `order_items`
- 字段名：snake_case，`created_at`, `user_id`
- 主键：`id` (UUID 或 自增ID)
- 外键：`{table_singular}_id`

**字段设计原则**：
- 每个表必须有 `id`, `created_at`, `updated_at`
- 敏感数据加密存储
- 大文本字段单独表存储
- 状态字段使用枚举或字典表

#### 数据库设计示例

```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    username VARCHAR(50) UNIQUE NOT NULL,
    avatar_url TEXT,
    role VARCHAR(20) DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 文章表
CREATE TABLE posts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    content TEXT,
    status VARCHAR(20) DEFAULT 'draft',
    view_count INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引优化
CREATE INDEX idx_posts_user_id ON posts(user_id);
CREATE INDEX idx_posts_status ON posts(status);
CREATE INDEX idx_posts_created_at ON posts(created_at DESC);
```

#### ER图模板

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│      users      │       │      posts      │       │     comments    │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │───┐   │ id (PK)         │       │ id (PK)         │
│ email           │   │   │ user_id (FK)    │───┐   │ user_id (FK)    │
│ password_hash   │   │   │ title           │   │   │ post_id (FK)    │
│ username        │   └───│ content         │   └───│ content         │
│ avatar_url      │       │ status          │       │ created_at      │
│ role            │       │ view_count      │       └─────────────────┘
│ created_at      │       │ created_at      │
│ updated_at      │       │ updated_at      │
└─────────────────┘       └─────────────────┘
```

### 阶段5：API设计

#### RESTful API规范

**URL设计**：
```
资源           GET          POST         PUT          DELETE
─────────────────────────────────────────────────────────────────
/users         获取用户列表   创建用户      批量更新      -
/users/:id     获取单个用户   -           更新用户      删除用户
/users/:id/posts    获取用户的所有文章
/posts         获取文章列表   创建文章      批量更新      -
/posts/:id     获取文章      -           更新文章      删除文章
```

**HTTP状态码**：
| 状态码 | 含义 | 使用场景 |
|--------|------|---------|
| 200 | OK | 成功获取/更新 |
| 201 | Created | 成功创建 |
| 204 | No Content | 成功删除 |
| 400 | Bad Request | 请求参数错误 |
| 401 | Unauthorized | 未认证 |
| 403 | Forbidden | 无权限 |
| 404 | Not Found | 资源不存在 |
| 500 | Internal Error | 服务器错误 |

**响应格式**：
```json
// 成功响应
{
  "success": true,
  "data": {
    "id": "uuid",
    "name": "张三",
    "email": "zhang@example.com"
  },
  "message": "操作成功"
}

// 错误响应
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "邮箱格式不正确",
    "details": [
      {"field": "email", "message": "必须是有效的邮箱地址"}
    ]
  }
}

// 分页响应
{
  "success": true,
  "data": [...],
  "pagination": {
    "page": 1,
    "pageSize": 20,
    "total": 100,
    "totalPages": 5
  }
}
```

**GraphQL API规范**（可选）：
```graphql
type Query {
  user(id: ID!): User
  users(page: Int, pageSize: Int): UserConnection
  post(id: ID!): Post
  posts(filter: PostFilter, page: Int, pageSize: Int): PostConnection
}

type Mutation {
  createUser(input: CreateUserInput!): User
  updateUser(id: ID!, input: UpdateUserInput!): User
  deleteUser(id: ID!): Boolean
  createPost(input: CreatePostInput!): Post
}

type User {
  id: ID!
  email: String!
  username: String!
  posts: [Post!]!
  createdAt: DateTime!
}

type Post {
  id: ID!
  title: String!
  content: String
  author: User!
  comments: [Comment!]!
  createdAt: DateTime!
}
```

### 阶段6：前端开发

#### 项目结构

```
src/
├── assets/              # 静态资源
│   ├── images/
│   ├── styles/
│   └── fonts/
├── components/          # 通用组件
│   ├── common/          # 基础组件 (Button, Input, Modal)
│   ├── layout/          # 布局组件 (Header, Sidebar, Footer)
│   └── business/         # 业务组件 (UserCard, PostItem)
├── composables/         # 组合式函数 (Vue) / Hooks (React)
├── layouts/             # 页面布局
├── pages/               # 页面组件
├── router/              # 路由配置
├── services/            # API服务
│   ├── api.ts           # API实例
│   ├── user.ts          # 用户相关API
│   └── post.ts          # 文章相关API
├── stores/              # 状态管理 (Pinia/Zustand/Redux)
├── types/               # TypeScript类型定义
├── utils/               # 工具函数
├── App.vue / App.tsx    # 根组件
└── main.ts / index.tsx  # 入口文件
```

#### 组件开发规范

**组件命名**：
- 基础组件：BaseButton, BaseInput, BaseModal
- 业务组件：UserCard, PostList, CommentSection
- 页面组件：HomePage, LoginPage, ProfilePage

**Props定义**（Vue 3 + TypeScript）：
```typescript
interface Props {
  title: string
  type?: 'primary' | 'secondary' | 'danger'
  size?: 'small' | 'medium' | 'large'
  loading?: boolean
  disabled?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  type: 'primary',
  size: 'medium',
  loading: false,
  disabled: false,
})
```

**组件模板**：
```vue
<template>
  <div class="user-card" :class="{ 'is-loading': loading }">
    <img 
      v-if="avatar" 
      :src="avatar" 
      :alt="name" 
      class="user-card__avatar"
    />
    <div v-else class="user-card__avatar-placeholder">
      {{ name?.charAt(0)?.toUpperCase() }}
    </div>
    <div class="user-card__info">
      <h3 class="user-card__name">{{ name }}</h3>
      <p class="user-card__email">{{ email }}</p>
    </div>
    <slot name="actions"></slot>
  </div>
</template>

<script setup lang="ts">
interface Props {
  name: string
  email: string
  avatar?: string
  loading?: boolean
}

defineProps<Props>()
</script>

<style scoped>
.user-card {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px;
  background: white;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.user-card.is-loading {
  opacity: 0.6;
  pointer-events: none;
}

.user-card__avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  object-fit: cover;
}
</style>
```

#### 状态管理

**Pinia Store示例**（Vue 3）：
```typescript
// stores/user.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { userApi } from '@/services/user'

export const useUserStore = defineStore('user', () => {
  // State
  const user = ref<User | null>(null)
  const token = ref<string | null>(localStorage.getItem('token'))
  const loading = ref(false)

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const userName = computed(() => user.value?.name || 'Guest')

  // Actions
  async function login(email: string, password: string) {
    loading.value = true
    try {
      const response = await userApi.login({ email, password })
      token.value = response.token
      user.value = response.user
      localStorage.setItem('token', response.token)
      return response
    } finally {
      loading.value = false
    }
  }

  async function fetchUser() {
    if (!token.value) return
    loading.value = true
    try {
      user.value = await userApi.getCurrentUser()
    } finally {
      loading.value = false
    }
  }

  function logout() {
    user.value = null
    token.value = null
    localStorage.removeItem('token')
  }

  return { user, token, loading, isLoggedIn, userName, login, fetchUser, logout }
})
```

### 阶段7：后端开发

#### 项目结构

```
backend/
├── src/
│   ├── config/           # 配置文件
│   │   ├── database.ts
│   │   ├── redis.ts
│   │   └── index.ts
│   ├── controllers/      # 控制器
│   │   ├── user.controller.ts
│   │   └── post.controller.ts
│   ├── middleware/       # 中间件
│   │   ├── auth.middleware.ts
│   │   ├── error.middleware.ts
│   │   └── validation.middleware.ts
│   ├── models/           # 数据模型
│   │   ├── user.model.ts
│   │   └── post.model.ts
│   ├── routes/           # 路由
│   │   ├── user.routes.ts
│   │   └── post.routes.ts
│   ├── services/         # 业务逻辑
│   │   ├── user.service.ts
│   │   └── post.service.ts
│   ├── utils/            # 工具函数
│   │   ├── logger.ts
│   │   └── helpers.ts
│   ├── validators/       # 请求验证
│   │   ├── user.validator.ts
│   │   └── post.validator.ts
│   ├── app.ts            # 应用入口
│   └── server.ts         # 服务器启动
├── tests/                # 测试文件
├── prisma/               # Prisma ORM (如果使用)
├── package.json
└── tsconfig.json
```

#### 控制器开发

```typescript
// controllers/user.controller.ts
import { Request, Response, NextFunction } from 'express'
import { userService } from '@/services/user.service'
import { ApiError } from '@/utils/api-error'

export class UserController {
  async getUsers(req: Request, res: Response, next: NextFunction) {
    try {
      const { page = 1, pageSize = 20 } = req.query
      const result = await userService.getUsers({
        page: Number(page),
        pageSize: Number(pageSize),
      })
      res.json({
        success: true,
        data: result.data,
        pagination: result.pagination,
      })
    } catch (error) {
      next(error)
    }
  }

  async getUserById(req: Request, res: Response, next: NextFunction) {
    try {
      const { id } = req.params
      const user = await userService.getUserById(id)
      if (!user) {
        throw new ApiError(404, '用户不存在')
      }
      res.json({ success: true, data: user })
    } catch (error) {
      next(error)
    }
  }

  async createUser(req: Request, res: Response, next: NextFunction) {
    try {
      const userData = req.body
      const user = await userService.createUser(userData)
      res.status(201).json({ success: true, data: user })
    } catch (error) {
      next(error)
    }
  }

  async updateUser(req: Request, res: Response, next: NextFunction) {
    try {
      const { id } = req.params
      const userData = req.body
      const user = await userService.updateUser(id, userData)
      if (!user) {
        throw new ApiError(404, '用户不存在')
      }
      res.json({ success: true, data: user })
    } catch (error) {
      next(error)
    }
  }

  async deleteUser(req: Request, res: Response, next: NextFunction) {
    try {
      const { id } = req.params
      await userService.deleteUser(id)
      res.status(204).send()
    } catch (error) {
      next(error)
    }
  }
}

export const userController = new UserController()
```

#### 服务层开发

```typescript
// services/user.service.ts
import { prisma } from '@/config/database'
import { hashPassword, comparePassword } from '@/utils/crypto'
import { ApiError } from '@/utils/api-error'

export class UserService {
  async getUsers(params: { page: number; pageSize: number }) {
    const { page, pageSize } = params
    const skip = (page - 1) * pageSize

    const [users, total] = await Promise.all([
      prisma.user.findMany({
        skip,
        take: pageSize,
        orderBy: { createdAt: 'desc' },
        select: {
          id: true,
          email: true,
          username: true,
          avatar: true,
          role: true,
          createdAt: true,
        },
      }),
      prisma.user.count(),
    ])

    return {
      data: users,
      pagination: {
        page,
        pageSize,
        total,
        totalPages: Math.ceil(total / pageSize),
      },
    }
  }

  async getUserById(id: string) {
    return prisma.user.findUnique({
      where: { id },
      select: {
        id: true,
        email: true,
        username: true,
        avatar: true,
        role: true,
        createdAt: true,
        updatedAt: true,
      },
    })
  }

  async createUser(data: CreateUserInput) {
    const existingUser = await prisma.user.findFirst({
      where: {
        OR: [{ email: data.email }, { username: data.username }],
      },
    })

    if (existingUser) {
      throw new ApiError(400, '邮箱或用户名已存在')
    }

    const hashedPassword = await hashPassword(data.password)

    return prisma.user.create({
      data: {
        email: data.email,
        username: data.username,
        passwordHash: hashedPassword,
      },
    })
  }

  async updateUser(id: string, data: UpdateUserInput) {
    return prisma.user.update({
      where: { id },
      data,
    })
  }

  async deleteUser(id: string) {
    return prisma.user.delete({
      where: { id },
    })
  }
}

export const userService = new UserService()
```

#### 认证与授权

```typescript
// middleware/auth.middleware.ts
import { Request, Response, NextFunction } from 'express'
import { verifyToken } from '@/utils/jwt'
import { userService } from '@/services/user.service'

export interface AuthRequest extends Request {
  user?: {
    id: string
    email: string
    role: string
  }
}

export async function authMiddleware(
  req: AuthRequest,
  res: Response,
  next: NextFunction
) {
  try {
    const authHeader = req.headers.authorization
    if (!authHeader || !authHeader.startsWith('Bearer ')) {
      throw new ApiError(401, '未提供认证令牌')
    }

    const token = authHeader.substring(7)
    const decoded = verifyToken(token)

    const user = await userService.getUserById(decoded.userId)
    if (!user) {
      throw new ApiError(401, '用户不存在')
    }

    req.user = {
      id: user.id,
      email: user.email,
      role: user.role,
    }

    next()
  } catch (error) {
    next(new ApiError(401, '认证失败'))
  }
}

export function requireRole(...roles: string[]) {
  return (req: AuthRequest, res: Response, next: NextFunction) => {
    if (!req.user) {
      return next(new ApiError(401, '未认证'))
    }

    if (!roles.includes(req.user.role)) {
      return next(new ApiError(403, '权限不足'))
    }

    next()
  }
}
```

### 阶段8：测试

#### 测试金字塔

```
         ┌───────────┐
         │   E2E测试   │  ← 少量，验证关键流程
         ├───────────┤
         │  集成测试   │  ← 中等，验证模块协作
         ├───────────┤
         │  单元测试   │  ← 大量，验证单个函数/组件
         └───────────┘
```

#### 单元测试示例

**Jest + Vue Test Utils**：
```typescript
// components/UserCard.spec.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import UserCard from './UserCard.vue'

describe('UserCard', () => {
  it('renders user information correctly', () => {
    const wrapper = mount(UserCard, {
      props: {
        name: '张三',
        email: 'zhang@example.com',
        avatar: 'https://example.com/avatar.jpg',
      },
    })

    expect(wrapper.find('.user-card__name').text()).toBe('张三')
    expect(wrapper.find('.user-card__email').text()).toBe('zhang@example.com')
    expect(wrapper.find('img').attributes('src')).toBe('https://example.com/avatar.jpg')
  })

  it('shows placeholder when no avatar', () => {
    const wrapper = mount(UserCard, {
      props: {
        name: '李四',
        email: 'li@example.com',
      },
    })

    expect(wrapper.find('.user-card__avatar-placeholder').exists()).toBe(true)
    expect(wrapper.find('.user-card__avatar-placeholder').text()).toBe('李')
  })

  it('emits click event', async () => {
    const wrapper = mount(UserCard, {
      props: { name: '王五', email: 'wang@example.com' },
    })

    await wrapper.trigger('click')
    expect(wrapper.emitted('click')).toBeTruthy()
  })
})
```

**Jest + Supertest**（后端）：
```typescript
// tests/user.controller.spec.ts
import request from 'supertest'
import { app } from '@/app'
import { prisma } from '@/config/database'

describe('UserController', () => {
  beforeEach(async () => {
    await prisma.user.deleteMany({})
  })

  describe('GET /api/users', () => {
    it('returns empty array when no users', async () => {
      const response = await request(app)
        .get('/api/users')
        .expect(200)

      expect(response.body.success).toBe(true)
      expect(response.body.data).toEqual([])
    })

    it('returns paginated users', async () => {
      // 创建测试用户
      await prisma.user.createMany({
        data: [
          { email: 'user1@example.com', username: 'user1', passwordHash: 'hash' },
          { email: 'user2@example.com', username: 'user2', passwordHash: 'hash' },
        ],
      })

      const response = await request(app)
        .get('/api/users')
        .query({ page: 1, pageSize: 10 })
        .expect(200)

      expect(response.body.success).toBe(true)
      expect(response.body.data).toHaveLength(2)
      expect(response.body.pagination.total).toBe(2)
    })
  })

  describe('POST /api/users', () => {
    it('creates a new user', async () => {
      const response = await request(app)
        .post('/api/users')
        .send({
          email: 'new@example.com',
          username: 'newuser',
          password: 'password123',
        })
        .expect(201)

      expect(response.body.success).toBe(true)
      expect(response.body.data.email).toBe('new@example.com')
    })

    it('returns error for duplicate email', async () => {
      await prisma.user.create({
        data: {
          email: 'existing@example.com',
          username: 'existing',
          passwordHash: 'hash',
        },
      })

      await request(app)
        .post('/api/users')
        .send({
          email: 'existing@example.com',
          username: 'newuser',
          password: 'password123',
        })
        .expect(400)
    })
  })
})
```

### 阶段9：部署

#### Docker部署

**Dockerfile**（Node.js应用）：
```dockerfile
# 构建阶段
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# 运行阶段
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production

COPY --from=builder /app/dist ./dist
COPY --from=builder /app/package*.json ./
RUN npm ci --production

RUN addgroup -g 1001 -S nodejs && adduser -S nodejs -u 1001
USER nodejs

EXPOSE 3000
CMD ["node", "dist/server.js"]
```

**docker-compose.yml**：
```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/myapp
      - REDIS_URL=redis://cache:6379
    depends_on:
      - db
      - cache
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=myapp
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  cache:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
```

#### CI/CD流程

**GitHub Actions**：
```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run linter
        run: npm run lint
      
      - name: Run tests
        run: npm run test:coverage
      
      - name: Build
        run: npm run build

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: username/myapp:latest
```

### 阶段10：运维监控

#### 日志规范

```typescript
// utils/logger.ts
import winston from 'winston'

export const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  defaultMeta: { service: 'myapp' },
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' }),
  ],
})

if (process.env.NODE_ENV !== 'production') {
  logger.add(
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.simple()
      ),
    })
  )
}
```

#### 监控指标

| 指标类型 | 指标名称 | 告警阈值 |
|---------|---------|---------|
| 可用性 | uptime | < 99.9% |
| 性能 | response_time_p99 | > 500ms |
| 性能 | request_rate | < 100 qps |
| 错误率 | error_rate | > 1% |
| 资源 | cpu_usage | > 80% |
| 资源 | memory_usage | > 85% |
| 资源 | disk_usage | > 90% |

## 快速启动模板

### 模板1：简单API服务

```
技术栈：Node.js + Express + PostgreSQL + Prisma

步骤：
1. 初始化项目
2. 配置数据库
3. 定义数据模型
4. 创建CRUD API
5. 添加认证中间件
6. 编写测试
7. Docker部署
```

### 模板2：前后端分离应用

```
技术栈：Vue 3 + Vite + FastAPI + PostgreSQL

步骤：
1. 创建前端项目 (Vue 3 + TypeScript)
2. 创建后端项目 (FastAPI)
3. 设计数据库模型
4. 实现REST API
5. 前端调用API
6. 添加状态管理
7. 部署前端静态资源
```

### 模板3：微服务架构

```
技术栈：Go + gRPC + Docker + Kubernetes

步骤：
1. 设计服务拆分
2. 定义Proto文件
3. 实现各服务
4. 配置服务发现
5. 配置API网关
6. 部署到K8s
7. 配置监控
```

## 资源索引

### 官方文档
- [Vue.js](https://vuejs.org/) - 前端框架
- [React](https://react.dev/) - 前端框架
- [Node.js](https://nodejs.org/) - 后端运行时
- [PostgreSQL](https://www.postgresql.org/) - 数据库
- [Docker](https://docs.docker.com/) - 容器化
- [TypeScript](https://www.typescriptlang.org/) - 类型系统

### 学习资源
- [MDN Web Docs](https://developer.mozilla.org/) - Web开发文档
- [RESTful API设计](https://restfulapi.net/) - API设计最佳实践
- [Prisma文档](https://prisma.io/docs) - ORM框架

## 操作步骤

### 快速开发流程

1. **需求确认**：与用户明确功能需求和约束
2. **技术选型**：根据场景选择合适的技术栈
3. **项目初始化**：创建项目结构，安装依赖
4. **数据库设计**：设计数据模型和表结构
5. **API开发**：实现后端API接口
6. **前端开发**：实现用户界面
7. **前后端联调**：确保前后端集成正常
8. **测试验证**：编写和运行测试
9. **部署上线**：部署到生产环境
10. **监控维护**：监控运行状态，持续优化

### 代码质量检查清单

- [ ] TypeScript类型完整，无any
- [ ] 组件/函数单一职责
- [ ] 统一的代码风格（ESLint/Prettier）
- [ ] 关键逻辑有注释说明
- [ ] 错误处理完善
- [ ] 安全措施到位（输入验证、SQL注入防护）
- [ ] 测试覆盖核心功能
- [ ] 性能考虑（索引、缓存）
- [ ] 文档完整（README、API文档）

## 注意事项

- **需求优先**：先明确需求，避免过度设计
- **敏捷迭代**：小步快跑，快速验证
- **代码质量**：保持代码整洁，可维护性优先
- **安全第一**：始终考虑安全性
- **性能意识**：从一开始考虑性能优化
- **自动化测试**：重视测试，保证质量
- **文档同步**：代码和文档保持同步

## 适用场景

**最适合**：
- 需要完整前后端的应用开发
- 需要快速交付MVP
- 需要技术选型和架构设计
- 需要规范化开发流程

**不太适合**：
- 纯前端或纯后端任务（使用专门的技能）
- 已有架构只需局部修改
- 简单的脚本或工具开发
