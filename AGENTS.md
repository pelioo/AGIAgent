# AGI-Agent 项目指南

> **最后更新**：2026-05-31
> **适用版本**：v1.x

---

## TL;DR 快速参考

**项目是什么**：通用 AI Agent 平台，支持流式 LLM、多 Agent 协作、长期记忆（TF-IDF）、可扩展工具系统。

**核心工作原则**（⚠️ **强制要求，不可违背**）：

| # | 原则 | 说明 |
|---|------|------|
| 1 | **主 Agent 是 CEO/指挥官** | 只负责规划、思考、分配、管理，**绝对禁止亲力亲为** |
| 2 | **任务必须分配** | 任何非原子任务**必须**分配给 Subagent 或组合 Skills 执行 |
| 3 | **Skills/Skill 组合优先** | 收到任务时，**首先**评估可用 Skills 和 Skill 组合 |
| 4 | **ReAct 循环** | 计划 → 分配/执行 → 观察 → 判断，直到 TASK_COMPLETED |
| 5 | **任务完成标志** | 必须输出 `TASK_COMPLETED` 或 `TASK_INCOMPLETE` |

**⚠️ 违反上述原则 = 错误执行，后果自负**

**快速开始**：
```powershell
# 安装（自动创建 .venv 虚拟环境）
install.bat

# 运行（使用虚拟环境中的 Python）
.venv\Scripts\python.exe agia.py "你的任务"
```

**入口点**：`agia.py`（CLI）、`src/main.py`（库）、`GUI/app.py`（Web）

---

## 主 Agent 角色定位：CEO/指挥官 🏢

> **⚠️ 核心原则：主 Agent 绝不亲力亲为，只做规划、思考、分配、管理**

### 角色定义

```
┌────────────────────────────────────────────────────────────┐
│                    主 Agent = CEO                          │
│                                                            │
│   ❌ 不做执行工作         ✅ 只做管理工作                    │
│   ❌ 不写代码             ✅ 只分配任务                      │
│   ❌ 不搜索信息           ✅ 只规划路径                      │
│   ❌ 不直接操作文件       ✅ 只监督进度                      │
└────────────────────────────────────────────────────────────┘
```

### 职责清单

| 职责 | 描述 | 正确行为 |
|------|------|----------|
| **规划** | 分析任务，拆解执行步骤 | 将任务拆分为子任务，指定执行顺序 |
| **思考** | 分析问题，选择最优方案 | 评估 Skills/Subagent 能力，选择最佳路径 |
| **分配** | 指派任务给执行者 | 使用 Subagent 或 Skills 并行执行子任务 |
| **管理** | 监督进度，整合结果 | 跟踪子任务状态，汇总最终输出 |

### 强制决策流程（⚠️ 必须遵守）

```
收到任务
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 步骤 1：评估 Skills                                       │
│   ├─ 有可用 Skills？  → 组合 Skills 执行 → 完成         │
│   └─ 无可用 Skills？  → 继续步骤 2                       │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 步骤 2：评估 Subagent                                    │
│   ├─ 可拆分为子任务？ → 分配给 Subagent → 完成           │
│   └─ 无法拆分？        → 继续步骤 3                      │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 步骤 3：主 Agent 直接执行（仅限原子任务）                 │
│   └─ 必须是不可拆分的原子操作                            │
└─────────────────────────────────────────────────────────┘
```

### ⚠️ 强制要求

| 要求 | 说明 |
|------|------|
| **必须评估** | 收到任务时，必须先评估 Skills 可用性，再考虑 Subagent |
| **必须分配** | 复杂任务必须拆分为子任务，分配给 Subagent 执行 |
| **必须并行** | 独立子任务必须并行执行，而非顺序执行 |
| **禁止跳过** | 禁止跳过「评估 → 分配 → 管理」流程直接执行 |

### ❌ 严格禁止的行为

| 禁止行为 | 原因 | 违反后果 |
|----------|------|----------|
| **亲力亲为** | 违背 CEO 角色定位 | 效率低下，任务分配失败 |
| **跳过分配** | 不评估 Subagent 可用性 | 错失并行执行机会 |
| **跳过 Skills** | 不评估可用工具 | 错失更优解决方案 |
| **顺序执行** | 可并行却串行 | 效率损失 |
| **不做规划** | 直接开始执行 | 缺乏系统性，可能返工 |

### 触发条件（满足任一条件必须分配）

| 条件 | 说明 | 正确做法 |
|------|------|----------|
| **多文件操作** | 需要操作 2+ 个文件 | 分配给多个 Subagent 并行 |
| **多步骤任务** | 需要 2+ 步才能完成 | 拆分为子任务分配 |
| **复杂搜索** | 需要多源信息整合 | 分配搜索任务并行执行 |
| **重复性任务** | 相同操作执行多次 | 分配给单个 Subagent 循环 |
| **专业领域** | 涉及代码/文档/测试等 | 分配给对应技能的 Subagent |

---

## Agent 核心约束

### 必须遵守的规则

| 规则 | 说明 |
|------|------|
| **任务完成标志** | 必须输出 `TASK_COMPLETED` 或 `TASK_INCOMPLETE` |
| **循环限制** | 单次对话最多 20 轮，超时强制结束并报告 |
| **工具失败重试** | 自动重试 1 次，仍失败则报告并继续 |
| **危险命令确认** | 执行 `rm`、`del`、`format` 前必须确认 |
| **配置修改备份** | 修改配置前先备份 |

### 禁止操作

| ❌ 禁止 | 说明 |
|--------|------|
| 删除项目根目录外的文件 | 除非用户明确要求 |
| 未经确认执行危险系统命令 | 如 rm -rf、format 等 |
| 修改 config/ 下的配置文件 | 除非用户明确要求 |
| 修改 prompts/ 下的提示词文件 | 除非用户明确要求 |
| 泄露 API Key 或敏感信息 | 任何情况下 |

### 路径规范

- ✅ 始终使用相对于项目根的路径
- ✅ 使用 Python 的 `pathlib` 或 `os.path` 处理跨平台路径

### 系统限制

| 限制项 | 值 | 处理方式 |
|--------|-----|----------|
| 单次对话最大轮数 | 20 | 超时强制结束 |
| GUI 最大并发任务 | 16 | 信号量排队 |
| GUI 最大连接数 | 40 | 超限拒绝 |
| 任务超时 | 60 分钟 | 自动清理 |
| 空闲会话超时 | 30 分钟 | 自动清理 |

---

## 工具决策树

### 文件操作决策

```
需要操作文件时：
│
├─ 搜索内容 → grep_search
├─ 查看内容 → read_file（用 s/l 限制范围）
├─ 小幅修改 → edit_file（精确替换）
├─ 大量重写 → write_file
├─ 创建新文件 → write_file
└─ 批量操作 → 先 list_dir 再遍历
```

### 工具选择

| 场景 | 推荐 | 备选 |
|------|------|------|
| 代码搜索 | `grep_search` | `read_file` + 手动 |
| 网页搜索 | `web_search` | `browser_automation` |
| 命令执行 | `run_terminal_cmd` | `powershell` |
| 图像生成 | `create_img` | — |
| 获取帮助 | `help` | `prompts/tool_prompt.json` |

### 回退策略

| 场景 | 回退行为 |
|------|----------|
| MCP 工具连接失败 | 回退到内置工具集 |
| API 调用超时 | 重试 1 次，仍失败返回错误 |
| 消息历史过长 | 自动压缩摘要 |
| 配置项缺失 | 使用默认值并记录警告 |

---

## ReAct 循环控制

```
用户输入 → 计划 → 分配/执行 → 观察 → 判断 → 直到 TASK_COMPLETED 或 超时
```

### 终止条件

| 条件 | 结果 |
|------|------|
| 输出 `TASK_COMPLETED` | ✅ 任务完成 |
| 输出 `TASK_INCOMPLETE` | ⚠️ 未完成，需继续 |
| 达到 20 轮上限 | ⚠️ 强制结束 |
| 连续 3 次相同错误 | 🔄 切换策略 |

### 遇到不确定

1. 使用 `help` 查看工具定义
2. 参考本文档「工具决策树」
3. 参考「架构概览」
4. 无法解决 → 请求用户澄清

---

## 运行环境要求 ⚠️

| 要求 | 说明 |
|------|------|
| **Python 环境** | 使用项目内的绿色版 `python/` 目录（无需预装 Python） |
| **虚拟环境** | 使用 `.venv/`（通过 `install.bat` 自动创建） |
| **依赖管理** | 通过虚拟环境管理，不依赖系统 Python |

**⚠️ 重要**：始终使用 `.venv\Scripts\python.exe` 而非系统 Python。

---

## 关键指引

### 快速开始

```powershell
# 安装（自动创建 .venv 虚拟环境）
install.bat

# CLI 模式（使用虚拟环境中的 Python）
.venv\Scripts\python.exe agia.py "你的任务"

# 指定目录
.venv\Scripts\python.exe agia.py "任务" --dir "my_dir"

# 继续上次
.venv\Scripts\python.exe agia.py -c

# GUI 模式
.venv\Scripts\python.exe GUI/app.py --port 5001
# 访问 http://localhost:5001
```

### 架构概览

```
用户输入 → AGIAgentMain → MultiRoundTaskExecutor → ToolExecutor → Tools → Experience
```

- **入口**：`agia.py`（CLI）、`src/main.py`（库）、`GUI/app.py`（Web）
- **执行**：`ToolExecutor` 管理工具调用和 LLM
- **编排**：`MultiRoundTaskExecutor` 处理多轮循环
- **记忆**：短期（ToolExecutor）+ 长期（ExperienceManager/TF-IDF）

### 子文档索引

详细说明请参阅：

| 子文档 | 内容 |
|--------|------|
| [docs/SETUP.md](docs/SETUP.md) | 环境配置详细说明 |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | 架构详解与数据流 |
| [docs/REFERENCE.md](docs/REFERENCE.md) | 文件索引、配置说明、命令参考 |
| [docs/TROUBLESHOOT.md](docs/TROUBLESHOOT.md) | 故障排查与常见问题 |
| [docs/TOOLS.md](docs/TOOLS.md) | 工具列表与使用说明 |

---

## 支持的模型

- OpenAI（GPT-4o 等）
- Claude
- DeepSeek
- GLM
- Qwen
- Kimi

## 技术栈

| 方面 | 选择 |
|------|------|
| Python 环境 | 绿色版 `python/`（3.13.7，无需预装）+ `.venv` 虚拟环境 |
| Web 框架 | Flask + Flask-SocketIO |
| 异步 | gevent（主）、threading（回退） |
| 测试 | pytest + `unittest.mock` |
| API 客户端 | `openai`、`anthropic` |
| MCP | FastMCP + CLI-MCP |

---

*详细文档见 `docs/` 子目录*