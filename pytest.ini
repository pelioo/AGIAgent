[pytest]
# pytest-asyncio configuration: only run event loop for tests explicitly marked
# with @pytest.mark.asyncio. This is required for Playwright sync API tests,
# which cannot run inside an asyncio event loop.
asyncio_mode = auto

# Test discovery
testpaths = tests

# Default options
addopts = -v --tb=short
